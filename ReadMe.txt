Instructions.
Run the Back end project from the root folder
In the project root folder, got to Front-end folder which contains client application.
then ==>
	run the following commands in terminal:
					yarn install
					yarn start


To add an order - http://localhost:3000/add-order
To view all orders - http://localhost:3000/view-order
